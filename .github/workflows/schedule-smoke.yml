name: Schedule Smoke Test

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm scheduler event
        run: echo "event=${{ github.event_name }} schedule=${{ github.event.schedule }}"
